def for methods 
render html : "text"

root 'controller_name#action_name' - > 'application#hello'

// first time git
git config --global user.name "Your Name"
git config --global user.email your.email@example.com

git checkout -f  // indo uncommited work

group :production do  // for production only gems
  gem 'pg', '0.18.4'
end


rails generate scaffold User name:string email:string  // for making models

rails db:migrate / migrate db after adding models

resources :users
resources :micropost   // models as resources

validates :content, length: { maximum: 140 },
                      presence: true // validate content


has_many :microposts 
belongs_to :user   // relationships   modify in "models file"

rails console
>> first_user = User.first // rails console																																																																																																																																																	

rails generate controller static_pages home help // build conroller
rails destroy  controller StaticPages home help  // destroy controller

rails db:migrate VERSION=0  // undo migration to a version

HTTp requestst ... Get , post

*test
get static_pages_home_url
    assert_response :success
    assert_select "title", "Home | Ruby on Rails Tutorial Sample App"


*TDD 'red-green-refactor'

rails test

layout file

embeded ruby "<% provide(:title, "Home") %> "
<title><%= yield(:title) %> | Ruby on Rails Tutorial Sample App</title>


















